[project]
name = "nightly-runner"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = "~=3.14.0"
dependencies = [
    "bottle>=0.13.4",
    "dbus-python>=1.4.0",
]

[dependency-groups]
dev = [
    "mypy>=1.20.1",
]
